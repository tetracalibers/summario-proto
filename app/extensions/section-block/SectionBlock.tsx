import { NodeViewWrapper, NodeViewContent } from "@tiptap/react"
import "./style.scss"

export default () => {
  return (
    <NodeViewWrapper className="section-block">
      <NodeViewContent />
    </NodeViewWrapper>
  )
}
